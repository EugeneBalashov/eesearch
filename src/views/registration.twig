{% extends 'base.twig' %}

{% block content %}

<form action="{{ path('registration') }}" method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p><input type="submit" /></p>
</form>

{% endblock content %}